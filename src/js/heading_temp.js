// subSectionDataArr.forEach((subSectionData) => {

              // const subSection = new SubSection(section.$container);

              // ENTIRE CHARTS BLOCK

              // let $featureBlock = $('.feature-value').first().clone(true, true);
              // let $featureValue = $featureBlock.find('.feature-value__header').not('.feature-value__header.is--phased');

              // $featureBlock.empty();

              // if(subSectionData.metric_type == 'currency' || subSectionData.metric_type == 'count') {

              //   $featureValue.find('.feature-value__label').text(subSectionData.name);
              //   if(sectionType == 'targets') {
              //     $featureValue.find('.feature-value__amount').text(subSectionData.value_target);
              //   } else {
              //     $featureValue.find('.feature-value__amount').text(subSectionData.value);
              //   }

              //   // ICON

              //   let $icons = $iconsTemplate.clone(true, true);

              //   let $iconWrapper = $featureValue.find('.feature-value__header_icon-wrapper');
              //   $iconWrapper.empty();

              //   $iconWrapper.append($icons.find('.icon--' + lookups["icon"][subSectionData.name]))
                  

              //   // DONUT ?

              //   let $progressContainer = $featureValue.find('.feature-value__header_chart-wrapper');
              //   $progressContainer.empty();

              //   if(subSectionData.metric_type == 'currency') {

              //     $progressContainer.remove();
              //   } else {

              //     new VizProgress(
              //       $progressContainer[0],0.2
              //     );
                
              //   }

              //   $featureBlock.append($featureValue);
              // }






              // subSection.$container.append($featureBlock);






              

            // })   